{
  "name": "spage-event-system",
  "version": "0.1.7",
  "description": "SPA Game Engine - event system",
  "author": "SPA",
  "license": "ISC",
  "main": "./lib/index.js",
  "types": "index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublish": "yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "rxjs": "^6.1.0",
    "spage-shared-interfaces": "^0.1.7"
  },
  "devDependencies": {
    "typescript": "^2.8.3"
  }
}
