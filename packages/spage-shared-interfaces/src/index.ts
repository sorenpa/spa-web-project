// TODO: Is this really needed? Cant we just export the interfaces directly from index.d.ts?
import { ComponentType, IComponent, IEntity, IMovable, IPlayer, ITransform, IVisible } from './interfaces'

export { ComponentType, IEntity, IComponent, IMovable, IPlayer, ITransform, IVisible }