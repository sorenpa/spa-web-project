{
  "name": "spage-input-system",
  "version": "0.1.3",
  "description": "SPA Game Engine - input system",
  "author": "SPA",
  "license": "ISC",
  "main": "./lib/index.js",
  "types": "index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublish": "yarn run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "spage-event-system": "^0.1.3",
    "spage-shared-interfaces": "^0.1.3"
  },
  "devDependencies": {
    "typescript": "^2.8.3"
  }
}
