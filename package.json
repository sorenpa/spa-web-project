{
	"private": true,
	"scripts": {
		"start": "lerna bootstrap && lerna run build --parallel --stream && lerna run start:react"
	},
	"devDependencies": {
		"lerna": "^2.11.0",
		"tslint": "^5.9.1",
		"typescript": "^2.7.2"
	},
	"workspaces": [
		"packages/*"
	  ]
}
